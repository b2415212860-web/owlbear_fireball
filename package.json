{
  "name": "owlbear-fireball",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0 --configLoader runner",
    "build": "tsc --noEmit && vite build --configLoader runner",
    "check": "tsc --noEmit",
    "test": "node --test src/geometry.test.ts src/protocol.test.ts tests/background.test.mjs tests/button.test.mjs tests/native-targeting.test.mjs tests/controller.test.mjs tests/fx.test.mjs tests/renderer.test.mjs",
    "test:deployment": "npm run build && node --test tests/deployment.test.mjs",
    "preview": "vite preview --host 0.0.0.0 --configLoader runner"
  },
  "dependencies": {
    "@owlbear-rodeo/sdk": "^3.1.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/three": "^0.185.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
